"use strict";

const process = require("./process");
const auth = require("./auth");
const users = require("./users");
const typeOfPermits = require("./typeOfPermits");

module.exports = { process, auth, users, typeOfPermits };
